import * as users from './users';
import * as auth from './auth';
import * as deficiencies from './deficiencies';
import * as places from './places';

export const api = {
  auth,
  deficiencies,
  places,
  users,
};
